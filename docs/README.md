# docs

In-repo documentation that ships with the codebase (CLAUDE.md → Folder Responsibilities).

## Contents

- `ARCHITECTURE.md` — the approved folder-tree architecture for this project (this session)
- `sessions/` — per-session HTML changelogs, once `scripts/generateSessionChangelog.js` exists (see `scripts/README.md`)
- `screenshots/` — production screenshots for review docs, once portal UI work ships

## Never goes here

- Portal-generated state maps — those live in `/system` and are do-not-touch
